# ── 4CBON2 Gemini Space ─────────────────────────────────────
# Verified set: `pip check` passes with gradio 5.50.0 / google-genai 2.8.0 /
# chromadb 1.5.9 / pydantic 2.12.3.
#
# The pydantic pin matters: an unpinned google-genai resolves pydantic 2.13.x,
# which gradio 5.50.0 rejects (it requires pydantic<=2.12.3) and the Space build
# fails. Keep this line.

gradio>=5.0.0,<6
pydantic<=2.12.3

# Google Generative AI API (modern unified SDK — replaces Colab's google.colab.ai)
google-genai>=1.0.0,<3

# Vector store for the AI / Mathematics / Science research databases
chromadb>=0.4.24

# Document ingestion
PyPDF2>=3.0.0
python-docx>=1.1.0

# Retrieval tools
duckduckgo-search>=6.1.12
beautifulsoup4>=4.12.0
requests>=2.31.0

# Output artifacts + dashboard
fpdf2>=2.7.0
plotly>=5.20.0
